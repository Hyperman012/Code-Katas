{
  "name": "kodykatas",
  "version": "1.0.0",
  "description": "package to use when doing katas",
  "scripts": {
    "test": "mocha -r ts-node/register tests/**/*.spec.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "ts-node": "^10.9.2",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2"
  }
}
