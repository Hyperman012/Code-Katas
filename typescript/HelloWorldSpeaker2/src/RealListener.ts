import { IListener } from "./IListener";

export class RealListener implements IListener {

    Listen(whatISay: string): void {
        /*
        * Enormously complex code that does strange
        * and wonderous things goes here!
        */
    }

}